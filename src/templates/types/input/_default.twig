{% import "_includes/forms" as forms %}

{{ forms.textField({
    id: name ~ link.type|id ~ '-value',
    name: name ~ '[values]['~ link.type ~']',
    value: currentLink.value ?? '',
    placeholder: link.defaultPlaceholder,
    errors: link.getErrors('value') ?? null
}) }}

