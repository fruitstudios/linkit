{% import "_includes/forms" as forms %}

{% include 'linkit/types/settings/_element' %}

{{ forms.textField({
    label: "User Path"|t('linkit'),
    instructions: "Enter the path that you would like to use to link to each user. Note that the path can contain variables like {username} or {id}."|t('linkit', { type: type.defaultLabel|lower }),
    id: type.type|id~'-userPath',
    name: 'userPath',
    value: type.userPath,
    placeholder: type.defualtUserPath,
    errors: type.getErrors('userPath')
}) }}
